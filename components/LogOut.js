import React from 'react';
import { View, Button } from 'react-native';

const LogOut = props => {
    return (
        <View style={{ flex: 1, borderColor: 'black', borderWidth: 1 }}>
            <Button title="Log Out C" />
        </View>
    )
}

export default LogOut;